# Darwin pollfd: int fd, short events, short revents. The two short fields
# use byte arrays because Neri has no 16-bit scalar. Darwin is little endian.
struct BenchmarkPollDescriptor
  fd: Int32
  events: Byte[2]
  returned: Byte[2]
end
