use buffers
use host
use result
use tasks
namespace process

class EnvironmentEntry
  public name: String
  public value: String
  def init(name: String, value: String): Void
    this.name = name
    this.value = value
  end
end
