namespace arrays

## Builds an array in index order on the calling thread. The callback may
## capture mutable values; each returned value is stored without copying it.
@operation("arrays.generate")
def generate<T>(count: Int, callback: fn(Int): T): T[]
end
