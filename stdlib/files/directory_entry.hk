use host
use tasks
use result
namespace files

class DirectoryEntry
  public name: String
  public kind: readonly FileKind
  def init(name: String, kind: readonly FileKind): Void
    this.name = name
    this.kind = kind
  end
end
