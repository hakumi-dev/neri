use buffers
use host
use tasks
use result
namespace crypto

class Failure
  private value: String

  private def init(code: String): Void
    this.value = code
  end

  static def fromCode(code: String): Failure
    return new Failure(code)
  end

  def code(): String
    return this.value
  end
end
