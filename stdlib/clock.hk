namespace clock

@cabi("neri_rt_v1_clock_milliseconds")
def unsafe nativeMilliseconds(): Int
end

# An arbitrary monotonic epoch, suitable for elapsed time and deadlines.
def milliseconds(): Int?
  unsafe
    let value = nativeMilliseconds()
    if value < 0
      return null
    end
    return value
  end
end
