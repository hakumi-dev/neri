use host
use result
use resources
namespace http

class ParseFailure
  public cause: result::Failure
  public status: Int
  public head: Bool

  def init(cause: result::Failure, status: Int, head: Bool): Void
    this.cause = cause
    this.status = status
    this.head = head
  end
end
