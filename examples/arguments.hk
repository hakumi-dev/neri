use ConsoleInteraction
use host

def main(): Void
  var index = 0
  while index < host.argumentCount()
    let name = host.argumentAt(index)
    if name != null
      ConsoleInteraction.println("Hello, " + name + "!")
    end
    index = index + 1
  end
end
