use ConsoleInteraction

def sum(numbers: Int[]): Int
  var total = 0
  for number in numbers
    total = total + number
  end
  return total
end

def main(): Void
  let numbers = [10, 20, 30]
  ConsoleInteraction.println("Total: " + (sum(numbers) as String))
end
