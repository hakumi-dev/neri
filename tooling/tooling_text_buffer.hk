use console
use host

class ToolingTextBuffer
  public first: ToolingTextNode?
  public last: ToolingTextNode?
  public count: Int = 0

  def add(value: String): Void
    let node = new ToolingTextNode(value)
    let tail = this.last

    if tail == null
      this.first = node
    else
      tail.next = node
    end
    this.last = node
    this.count = this.count + 1
  end
end
