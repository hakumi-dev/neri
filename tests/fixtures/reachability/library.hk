use console

namespace reachability

@library("neri-reachability-library-that-does-not-exist")
@cabiImport("neri_reachability_missing_native")
unsafe def missingNative(): Int32
end

def unreachableNative(): Int32
  unsafe
    return missingNative()
  end
end

def helper(): Int
  return 41
end

def live(): Int
  return helper() + 1
end

def neverCalled(): Void
  console::println("NERI_REACHABILITY_DEAD_FUNCTION_SENTINEL")
end

def callbackApply(operation: fn(Int): Int): Int
  return operation(42)
end

def identity<T>(value: T): T
  return value
end

public abstract class Base
  public abstract def value(): Int
end

public class Child: Base
  public override def value(): Int
    return 40
  end
end

public class DeadDescriptor
end

@cabiExport("reachability_contract_export_root")
unsafe def exportedRoot(): Int32
  return 73 as Int32
end
