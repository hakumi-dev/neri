namespace alternatives_library

public enum Result<T>
  case Ok(value: T)
  case Error(message: String)
end

def lexicalMatch(value: Result<Int>): Int
  match value
    case Result.Ok(number)
      return number
    case Result.Error(message)
      return 0
  end
end
