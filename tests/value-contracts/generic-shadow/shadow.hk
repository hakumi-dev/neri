use resources
namespace shadow

def failure(code: String, message: String): Int
  return 7
end

def run(): Int
  let ordinary = failure
  let imported = resources::failure<Int, String>("generic remains available")

  if imported.closeFailures.count() != 0
    return 0
  end
  return ordinary("LOCAL", "ordinary function wins")
end
