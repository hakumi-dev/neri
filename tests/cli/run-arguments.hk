use console
use host

def main(): Void
  var index = 0
  while index < host.argumentCount()
    let argument = host.argumentAt(index)
    if argument != null
      console.println("[" + argument + "]")
    end
    index = index + 1
  end
  host.exit(7)
end
