class Customer
  public name: String = ""
end

def invalidOppositeBranch(value: Customer?): String
  return value != null ? value.name : value.name
end

def narrowingDoesNotLeak(value: Customer?): String
  let selected = value == null ? "absent" : value.name

  return value.name
end

def main(): Void
end
