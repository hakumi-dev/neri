use tasks

def main(): Void
  let weak: fn(Int): Int = fn(index)
    return index
  end
  let invalid = tasks.generate(2, weak)
  let unknown = tasks.generate(2) do |index|
    return index
  end
  let noValues: Void[] = tasks.generate(2) do |index|
    return
  end
  let badCount = tasks.generate<Int>("two", weak)
end
