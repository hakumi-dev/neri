namespace graph_registration_data

## Source: type:graph_registration_model.Note.
def mappingEntity_4_Note(): neri_data::EntityMapping
  return new neri_data::EntityMapping("graph_registration_model.Note", "notes", [new neri_data::MappingColumn("graph_registration_model.Note.id", "Int", "id"), new neri_data::MappingColumn("graph_registration_model.Note.entryId", "Int", "entry_id"), new neri_data::MappingColumn("graph_registration_model.Note.body", "String", "body")], "id")
end

## Source: type:graph_registration_model.Note.
def decodeEntity_4_Note(row: neri_data::RawRow): neri_data::DecodeResult<graph_registration_model::Note>
  var value_id: Int = 0

  match neri_data::decodeInt(row, "id")
    case neri_data::DecodeResult.Value(value)
      value_id = value
    case neri_data::DecodeResult.Failure(error)
      return neri_data::DecodeResult<graph_registration_model::Note>.Failure(error)
  end
  var value_entryId: Int = 0

  match neri_data::decodeInt(row, "entry_id")
    case neri_data::DecodeResult.Value(value)
      value_entryId = value
    case neri_data::DecodeResult.Failure(error)
      return neri_data::DecodeResult<graph_registration_model::Note>.Failure(error)
  end
  var value_body: String = ""

  match neri_data::decodeString(row, "body")
    case neri_data::DecodeResult.Value(value)
      value_body = value
    case neri_data::DecodeResult.Failure(error)
      return neri_data::DecodeResult<graph_registration_model::Note>.Failure(error)
  end
  let decodedEntity = new graph_registration_model::Note(id: value_id, entryId: value_entryId, body: value_body)

  return neri_data::DecodeResult<graph_registration_model::Note>.Value(decodedEntity)
end

## Source: type:graph_registration_model.Note.
def capture_Note(entity: graph_registration_model::Note): neri_data::RawRow
  return new neri_data::RawRow([new neri_data::RawColumn("id", neri_data::Cell.Int(entity.id)), new neri_data::RawColumn("entry_id", neri_data::Cell.Int(entity.entryId)), new neri_data::RawColumn("body", neri_data::Cell.Text(entity.body))])
end

## Source: type:graph_registration_model.Note.
def apply_Note(target: graph_registration_model::Note, source: graph_registration_model::Note): Void
  target.id = source.id
  target.entryId = source.entryId
  target.body = source.body
end

## Captures the mapped scalar values.
def snapshot_Note(entity: graph_registration_model::Note): fields of graph_registration_model::Note
  return neri_data::snapshot<graph_registration_model::Note>(id: entity.id, entryId: entity.entryId, body: entity.body)
end

## Source: field:graph_registration_model.Note.id.
def key_Note(entity: graph_registration_model::Note): Int
  return entity.id
end
