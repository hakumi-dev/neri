namespace migration_scaffold_data

## Entry point for the mapped entity query surfaces.
class MigrationScaffoldDatabase
  private changes: neri_data::ChangeContext
  private tracker_Item: neri_data::EntityTracker<migration_scaffold_model::Item>
  ## Source: type:migration_scaffold_model.Item.
  public items: neri_data::Query<migration_scaffold_model::Item>

  ## Borrows a provider for query execution and saving; the caller owns its lifetime.
  def init(provider: neri_data::Provider? = null): Void
    this.changes = new neri_data::ChangeContext(provider)
    this.tracker_Item = new neri_data::EntityTracker<migration_scaffold_model::Item>("items", "id", new neri_data::ResultColumns([new neri_data::ResultColumn("id", neri_data::ResultColumnKind.Int(), false), new neri_data::ResultColumn("label", neri_data::ResultColumnKind.Text(), false), new neri_data::ResultColumn("amount", neri_data::ResultColumnKind.Float(), false)]), capture_Item, apply_Item)
    this.changes.register(this.tracker_Item.changeSet())
    this.items = new neri_data::Query<migration_scaffold_model::Item>("items", new neri_data::Decoder<migration_scaffold_model::Item>(decodeEntity_4_Item, new neri_data::ResultColumns([new neri_data::ResultColumn("id", neri_data::ResultColumnKind.Int(), false), new neri_data::ResultColumn("label", neri_data::ResultColumnKind.Text(), false), new neri_data::ResultColumn("amount", neri_data::ResultColumnKind.Float(), false)])), mapping: mappingEntity_4_Item(), provider: provider, tracker: this.tracker_Item, changes: this.changes)
  end

  ## Saves tracked inserts, changes, and removals as one atomic batch.
  public def saveChanges(): neri_data::WriteResult
    return this.changes.saveChanges()
  end

  ## Marks a tracked root and its explicitly linked client-cascade descendants for removal.
  public def removeCascade_items(handle: neri_data::TrackedHandle<migration_scaffold_model::Item>): neri_data::QueryFailure?
    return neri_data::removeCascade(this.changes, handle)
  end

  ## Enrolls a reachable graph through mapped navigation members.
  public def addGraph_items(root: migration_scaffold_model::Item): neri_data::TrackHandleResult<migration_scaffold_model::Item>
    let traversal = new GraphTraversal_MigrationScaffoldDatabase(this.changes, this.tracker_Item)

    return traversal.add_items(root)
  end

  ## Detaches tracked entities without writing to the database.
  public def clear(): Void
    this.changes.clear()
  end

  public def beginTransaction(): neri_data::WriteResult
    return this.changes.beginTransaction()
  end

  public def commitTransaction(): neri_data::WriteResult
    return this.changes.commitTransaction()
  end

  public def rollbackTransaction(): neri_data::WriteResult
    return this.changes.rollbackTransaction()
  end
end
