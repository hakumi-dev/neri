use app_data
use model

def main(): Void
  let predicate: quote fn(Order): Bool = quote do |order: Order|: Bool
    return order.total > 0
  end
  let invalid = (new Database()).customers.matching(predicate)
end
