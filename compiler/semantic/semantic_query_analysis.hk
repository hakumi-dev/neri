# Facts cover declarations and the enclosing query scope, not validation of all
# function bodies. A missing model means that the query did not finish.
class SemanticQueryAnalysis
  public model: SemanticModel?
  public position: Int
  public enclosingSyntaxNode: Int = -1

  def init(position: Int): Void
    this.position = position
  end
end
