def semanticDeclaration(owner: BootstrapBinder, node: Int): readonly SyntaxDeclaration?
  let declaration = owner.syntax.declarationAt(node)

  if declaration == null
    owner.report("NR_INTERNAL_SYNTAX", node, "Internal compiler structural failure: declaration metadata is missing.")
  end
  return declaration
end
