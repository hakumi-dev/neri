def syntaxDeclarationKind(kind: readonly SyntaxKind): Bool
  return kind.isClass() || kind.isFunction() || kind.isMethod() || kind.isField() || kind.isContract() || kind.isStruct() || kind.isUnion()
end
