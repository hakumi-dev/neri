class AgentProfileService
  private root: String

  def init(root: String): Void
    this.root = root
  end

  def run(operationId: String): String
    return agentProfileResponse(operationId, "unavailable", "Profiling is unavailable in the bootstrap compiler.", this.root, "", "", 0, false, "")
  end
end
